package main

import "testing"

// will setup better implementation later. They are just added as placeholder as of now.
func TestCreate(t *testing.T){
    t.Logf("this should be ok as create function only setting the direct value")
}

func TestRead(t *testing.T){
    t.Logf("No implementation as read function is just reading the mapp value")
}